// Entry point compiled into the inline <script> in index.html by `scripts/theme-boot.ts`.
// Keep it to this: everything it needs lives in `themeBoot.ts`, which ExtensionLoader imports too.
import { bootTheme } from "./themeBoot";

bootTheme();
